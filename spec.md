# Webメモアプリ 仕様書

## 概要

テキスト入力・音声入力に対応し、マークダウン形式でメモを管理・GoogleドライブのObsidianフォルダへ同期するWebアプリケーション。

---

## 機能要件

### 1. メモ入力

#### 1-1. テキスト入力
- マークダウン記法に対応したテキストエディタを提供する
- リアルタイムプレビュー（エディタ・プレビューの2ペイン表示）
- 対応するマークダウン記法：
  - 見出し（H1〜H6）
  - 箇条書き・番号付きリスト
  - チェックボックス（`- [ ]` / `- [x]`）
  - 太字・斜体・打消し線
  - インラインコード・コードブロック
  - 引用（`>`）
  - 水平線
  - リンク・画像
  - テーブル

#### 1-2. 音声入力
- Web Speech API（SpeechRecognition）を使用してブラウザ上で音声認識
- 対応言語：日本語（`ja-JP`）をデフォルト、英語（`en-US`）切り替え可能
- マイクボタン押下で録音開始、再押下または無音タイムアウトで停止
- 認識テキストをエディタのカーソル位置に挿入
- 録音中はビジュアルフィードバック（アイコン点滅・音量レベル表示）を提供

### 2. メモ管理

#### 2-1. ファイル単位の管理
- 1メモ = 1ファイル（`.md` 拡張子）
- ファイル名は自動生成（`YYYY-MM-DD_HHmmss.md`）または手動入力
- ファイル名の変更・削除が可能

#### 2-2. メモ一覧
- サイドバーにメモ一覧を表示（ファイル名・更新日時）
- 検索バーによる全文検索
- 作成日時・更新日時・ファイル名でのソート

#### 2-3. 自動保存
- 入力停止から1秒後に自動保存（デバウンス処理）
- 保存状態（保存済み / 保存中 / 未保存）をUIで表示

### 3. Googleドライブ同期

#### 3-1. 認証
- Google OAuth 2.0 でユーザー認証
- 必要スコープ：`https://www.googleapis.com/auth/drive.file`
- 初回ログイン後はリフレッシュトークンをローカルストレージに保存し、自動再認証

#### 3-2. 同期先フォルダ設定
- 同期先のGoogleドライブフォルダパスを設定画面で指定（例: `Obsidian/MyVault`）
- フォルダが存在しない場合は自動作成
- 設定値はローカルストレージに永続化

#### 3-3. 同期動作
- **アップロード**：メモ保存時に自動でGoogleドライブへアップロード（上書き）
- **ダウンロード**：アプリ起動時・手動リフレッシュ時にGoogleドライブから差分取得
- **競合解決**：更新日時が新しいものを優先（Last Write Wins）
- **手動同期**：同期ボタンで任意タイミングで全件同期可能

#### 3-4. オフライン対応
- オフライン時はローカルキャッシュ（IndexedDB）で編集を継続
- オンライン復帰後に未同期の変更を自動アップロード

---

## 非機能要件

| 項目 | 要件 |
|------|------|
| 対応ブラウザ | Chrome 最新版、Edge 最新版（Web Speech API 対応ブラウザ） |
| レスポンシブ | PC・タブレット・スマートフォン対応 |
| パフォーマンス | メモ一覧1,000件以上でも一覧表示を1秒以内に描画 |
| セキュリティ | OAuthトークンはセキュアに管理、HTTPSのみ通信 |
| アクセシビリティ | キーボード操作のみで全機能が使用可能 |

---

## 技術スタック（案）

| レイヤー | 技術 |
|----------|------|
| フレームワーク | React + TypeScript |
| マークダウンエディタ | CodeMirror 6 |
| マークダウンパーサ | marked.js / remark |
| 音声認識 | Web Speech API（SpeechRecognition） |
| ローカルストレージ | IndexedDB（idb ライブラリ経由） |
| Googleドライブ連携 | Google Drive API v3 + Google Identity Services |
| スタイリング | Tailwind CSS |
| ビルドツール | Vite |

---

## 画面構成

```
+----------------------------------+
|  ヘッダー（アプリ名 / 同期状態 / 設定）  |
+----------+-----------------------+
| メモ一覧  |  エディタペイン          |
| サイドバー|  [マークダウン入力]      |
|          |                       |
| [検索]   |  ツールバー             |
| -------- |  (音声ボタン/書式ボタン) |
| memo1.md |                       |
| memo2.md +------+----------------+
| memo3.md |      | プレビューペイン  |
|          | 分割  |                 |
+----------+------+----------------+
```

### 主要画面
1. **メインエディタ画面**：サイドバー + エディタ + プレビューの3カラム構成
2. **設定画面**：Googleドライブフォルダパス・音声入力言語・テーマ（ライト/ダーク）
3. **Googleログインモーダル**：初回起動時または未認証時に表示

---

## データ仕様

### メモデータ（IndexedDB）

```typescript
interface Memo {
  id: string;           // UUID
  filename: string;     // 例: "2026-02-26_153000.md"
  content: string;      // マークダウン本文
  createdAt: number;    // Unix timestamp (ms)
  updatedAt: number;    // Unix timestamp (ms)
  driveFileId?: string; // GoogleドライブのファイルID（同期済みの場合）
  syncedAt?: number;    // 最終同期日時
  isDirty: boolean;     // 未同期の変更があるか
}
```

---

## 開発フェーズ

| フェーズ | 内容 |
|----------|------|
| Phase 1 | テキスト入力・マークダウン表示・ローカル保存（IndexedDB） |
| Phase 2 | 音声入力機能の実装 |
| Phase 3 | Googleドライブ認証・同期機能の実装 |
| Phase 4 | オフライン対応・UIブラッシュアップ |

---

## 制約・前提条件

- 音声入力はHTTPS環境（またはlocalhost）でのみ動作する
- Google Drive APIの利用にはGCPプロジェクトとOAuthクライアントIDの取得が必要
- ObsidianはGoogleドライブ上のフォルダをVaultとして利用する設定が別途必要
