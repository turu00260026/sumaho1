# stand.fm 音声要約アプリ 仕様書

## 概要

stand.fm のエピソードURLまたはRSSフィードを入力し、
AI（文字起こし + 要約）によって**約500文字の日本語要約**を生成するWebアプリ。
要約履歴はブラウザのIndexedDBに保存する。

---

## 機能要件

### 1. 入力方法

#### 1-1. エピソードURL入力
- stand.fm のエピソードURL（例: `https://stand.fm/episodes/xxxxxxxx`）を貼り付けて要約
- URLのバリデーション（stand.fm ドメインのみ受け付ける）

#### 1-2. RSSフィード登録
- チャンネルのRSS URL（`https://stand.fm/rss/{channel_id}`）を登録
- 登録済みチャンネルの最新エピソード一覧を表示
- 一覧から選択して要約を実行

### 2. 要約生成

1. バックエンド（サーバーレス関数）がstand.fmから音声ファイルを取得（CORS回避のためプロキシ経由）
2. 音声を文字起こしAPI（Whisper / Gemini 等）でテキスト化
3. テキストを要約API（GPT / Gemini 等）で**約500文字**に要約
4. 要約結果をフロントエンドに返す

### 3. 要約結果の表示

- エピソードタイトル・配信日・サムネイル
- 要約テキスト（500字前後）
- コピーボタン（クリップボードにコピー）
- 元エピソードへのリンク

### 4. 履歴管理

- 要約した内容はブラウザのIndexedDBに保存
- 履歴一覧画面：タイトル・配信日・要約冒頭・保存日時を表示
- 履歴の削除（個別 / 全件）
- 履歴検索（キーワードで要約本文を検索）

---

## 非機能要件

| 項目 | 要件 |
|------|------|
| 対応ブラウザ | Chrome / Edge / Safari 最新版 |
| レスポンシブ | PC・スマートフォン対応 |
| 要約時間 | 30分以内の音声を3分以内に要約完了 |
| セキュリティ | APIキーはサーバーサイドのみで保持、クライアントに露出しない |

---

## 技術スタック案

| レイヤー | 技術 |
|----------|------|
| フロントエンド | React + TypeScript |
| スタイリング | Tailwind CSS |
| ローカルDB | IndexedDB（idb ライブラリ） |
| バックエンド | Next.js API Routes または Cloudflare Workers |
| 文字起こし | OpenAI Whisper API **または** Gemini 1.5 Flash（音声直接入力） |
| 要約 | OpenAI GPT-4o-mini **または** Gemini 1.5 Flash |
| ビルド | Vite（フロントのみの場合） / Next.js（フルスタックの場合） |

> **APIの選択肢まとめ**
> - **OpenAI（Whisper + GPT-4o-mini）**：文字起こし精度が高い。2ステップ処理。
> - **Gemini 1.5 Flash**：音声ファイルをそのまま渡して要約まで1ステップ。コスト低め。

---

## 画面構成

```
+------------------------------------------+
|  ヘッダー（アプリ名）                         |
+------------------------------------------+
|  [URLを入力して要約]  [RSSチャンネル一覧]      |  ← タブ切り替え
+------------------------------------------+
|  stand.fm URL: [________________] [要約]   |
+------------------------------------------+
|  ▼ 要約結果                               |
|  タイトル：〇〇のトーク #123               |
|  配信日：2026-02-26                        |
|  ---------------------------------------- |
|  （約500字の要約テキスト）                   |
|                           [コピー] [リンク] |
+------------------------------------------+
|  ▼ 履歴                                   |
|  [検索__________]                         |
|  - 〇〇のトーク #123  2026-02-26  [削除]   |
|  - 〇〇のトーク #122  2026-02-20  [削除]   |
+------------------------------------------+
```

---

## データ仕様

### 要約履歴（IndexedDB）

```typescript
interface SummaryRecord {
  id: string;           // UUID
  episodeUrl: string;   // stand.fm エピソードURL
  title: string;        // エピソードタイトル
  publishedAt: string;  // 配信日（ISO 8601）
  thumbnail?: string;   // サムネイルURL
  summary: string;      // 要約テキスト（約500字）
  savedAt: number;      // 保存日時（Unix timestamp ms）
}
```

### RSSチャンネル登録（IndexedDB）

```typescript
interface Channel {
  id: string;           // UUID
  rssUrl: string;       // RSSフィードURL
  name: string;         // チャンネル名
  addedAt: number;      // 登録日時
}
```

---

## 制約・前提条件

- stand.fm の音声ファイルは直接ブラウザからアクセスできないため、バックエンド経由のプロキシが必須
- OpenAI / Gemini のAPIキーは別途取得が必要
- stand.fm のHTML構造変更によりURL解析が壊れる可能性があるため、RSS経由の利用を推奨
- 長時間エピソード（1時間超）は文字起こしに時間・コストがかかる点を留意

---

## 開発フェーズ

| フェーズ | 内容 |
|----------|------|
| Phase 1 | URLからの音声取得・文字起こし・要約表示（最小構成） |
| Phase 2 | 履歴保存・履歴一覧・検索 |
| Phase 3 | RSSチャンネル登録・エピソード一覧からの要約 |
| Phase 4 | UIブラッシュアップ・エラーハンドリング強化 |
